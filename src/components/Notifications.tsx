import * as React from 'react';

const Notifications = (props: any) => (
  <div>
    <nav>
      <span>すべて</span>
      <span>@ツイート</span>
    </nav>
    <ul>
      <li>a</li>
    </ul>
  </div>
);

export default Notifications;
